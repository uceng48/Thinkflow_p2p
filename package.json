{
  "name": "thinkflow-p2p",
  "version": "1.0.0",
  "description": "P2P database with camera scanner",
  "main": "index.js",
  "scripts": {
    "build": "capacitor copy android && capacitor sync android",
    "open": "capacitor open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}